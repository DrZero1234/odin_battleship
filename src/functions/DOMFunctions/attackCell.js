export function attackCell(player) {
    const gameboard = player.gameboard;

    let DOM_board = player.isHuman ? document.querySelector(".player-board") : document.querySelector(".cpu-board");
    console.log(DOM_board)

}